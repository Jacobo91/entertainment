import './NoResults.css';

const NoResults = () => {
    return (
        <div className="no-results-wrapper center-content">
            <p className="no-results-text">Ooooops, no results where found</p>
        </div>
    );
};

export default NoResults;
